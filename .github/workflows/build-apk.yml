name: Android AIDE - GitHub Actions Build APK

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test
        run: |
          echo "=================================="
          echo "ANDROID AIDE WORKFLOW TEST"
          echo "=================================="
          echo "Workflow dispatch is working."
          echo "Repository: $GITHUB_REPOSITORY"
          echo "Branch: $GITHUB_REF"
